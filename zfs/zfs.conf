# Repo:   zfs/zfs.conf
# Deploy: /etc/modprobe.d/zfs.conf on the Proxmox VE host (Chapter 2)
# Caps the ARC at 4 GiB (minimum 1 GiB) on the 32 GB reference host.
options zfs zfs_arc_min=1073741824
options zfs zfs_arc_max=4294967296
